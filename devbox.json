{
  "packages": [
    "mdbook@latest"
  ],
  "env": {
    "PATH": "$DEVBOX_PROJECT_ROOT/bin:$PATH",
    "build": "$DEVBOX_PROJECT_ROOT/book",
    "project": "$DEVBOX_PROJECT_ROOT"
  },
  "shell": {
    "init_hook": [
      "alias b='mdbook build'",
      "alias s='mdbook serve'"
    ]
  }
}
